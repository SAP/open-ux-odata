sap.ui.define(['sap/fe/core/AppComponent'], function (AppComponent) {
    'use strict';

    return AppComponent.extend('sales.orders.app.Component', {
        metadata: {
            manifest: 'json'
        }
    });
});
