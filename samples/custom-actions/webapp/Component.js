sap.ui.define(['sap/fe/core/AppComponent'], function (AppComponent) {
    'use strict';

    return AppComponent.extend('custom.actions.app.Component', {
        metadata: {
            manifest: 'json'
        }
    });
});
